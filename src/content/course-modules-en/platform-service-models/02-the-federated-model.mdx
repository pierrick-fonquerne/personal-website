---
course: platform-service-models
order: 2
title: The federated model
subtitle: 'A control plane that owns the topology, services that own their resources, and an idempotent reconciliation between the two.'
estimatedMinutes: 22
published: true
---

import Callout from '../../../components/learning/Callout.astro';
import Quiz from '../../../components/learning/Quiz.tsx';
import MermaidDiagram from '../../../components/learning/MermaidDiagram.tsx';

The previous chapter ended on a dilemma: the integrated monolith provides consistency but forbids autonomy of the building blocks; autonomous services provide autonomy but give up consistency. This chapter presents the synthesis that reconciles both, under one non-negotiable condition: accepting that the same concept exists _several times_, at different levels of responsibility.

By the end of this chapter, you will be able to describe the federated model, explain the split of responsibilities between control plane and services, and recognize the three technical mechanisms that make it possible: the opaque external reference, the idempotent upsert and the management marker.

## The central idea: separating topology from resources

The federated model rests on a precise sharing of sovereignty:

- **The control plane** is the source of truth for the **organizational topology**: organizations, projects, environments, who belongs to what, who has access to what.
- **Each service** is the source of truth for **its business resources**: flags for the feature flag service, repositories for the code service, releases for the deployment service.

And the bridge between the two: each service maintains a **local projection** of the topological entities that concern it. The feature flag service has _its own_ `project` table, minimal, which mirrors the central referential when a control plane exists, or gets filled manually when the service runs alone.

<MermaidDiagram
  client:visible
  definition={`flowchart TD
    CP[Control plane<br/><i>source of truth: Org, Project, Environment</i>]
    CP -- "provisions / reconciles<br/>(idempotent APIs)" --> A
    CP -- " " --> B
    CP -- " " --> C
    subgraph A[Feature flag service]
      A1[Project*, Environment*]
      A2[Flags ✦]
    end
    subgraph B[Source code service]
      B1[Project*]
      B2[Repositories ✦]
    end
    subgraph C[Deployment service]
      C1[Project*, Environment*]
      C2[Releases ✦]
    end
    style CP fill:#7c5cff,color:#fff`}
  caption="* local projection of the referential — ✦ resources the service is the source of truth for"
/>

Read the diagram carefully: it shows the most important property of the model. **Each service remains complete and autonomous**: its local projection of `Project` is enough to operate without a control plane. And **the platform remains consistent**: when the control plane exists, it is the one creating and synchronizing those projections. The same binary serves both scenarios: standalone product or integrated building block.

<Callout kind="warn" title="The acceptance condition">
  The federated model requires giving up a dogma: "a piece of data must exist in only one place".
  Here, the project exists in the control plane AND in every service. This is not accidental
  duplication, it is an **assumed projection**, with a clear owner (the control plane) and an
  explicit convergence mechanism (reconciliation). Ungoverned duplication is debt; governed
  projection is architecture.
</Callout>

## Mechanism 1: the opaque external reference

How does the control plane find "its" project inside each service? Through a dedicated field on each service's topological entities:

```text
Project {
  key:          "my-app"           // service-local identifier
  name:         "My application"
  external_ref: "platform:org/acme/project/my-app"    // optional
  managed_by:   "control-plane"                        // optional
}
```

`external_ref` is an identifier **set by the external system and never interpreted by the service**. The service indexes it (with a uniqueness constraint), returns it, but never reads its structure. It is the annotations pattern from container orchestration systems: a memo the external manager leaves to itself.

Why opaque? Because any interpreted structure becomes coupling. If the flag service _parsed_ the URI to extract the organization name, the URI format would become a schema contract between all systems, impossible to evolve. Kept opaque, it remains a simple link: and a link, unlike a schema, never breaks.

This is exactly the strategy of federated identity protocols: the subject identifier is defined as an opaque string. Twenty-five years of interoperability rest on that opacity.

## Mechanism 2: the idempotent upsert

The control plane does not "create" projections: it **reconciles**. Each service's API exposes an operation whose contract is: "make sure a project with this `external_ref` exists and looks like this".

- If no entity carries this reference: creation.
- If an entity already carries it: update of the projected fields.
- In all cases: **replaying the operation changes nothing**. That is the definition of idempotence.

This property changes the nature of synchronization. No need to guarantee that a provisioning message arrives exactly once (a costly guarantee, often illusory in distributed systems): being able to replay it is enough. The control plane can re-declare the complete desired state at every startup, after every incident, periodically as a safety net. The services' state **converges** towards the declared state, exactly like a configuration management system converges towards its manifest.

<Callout kind="tip" title="Declarative rather than imperative">
  Note the inversion: the control plane does not say "do this" (imperative, fragile,
  order-dependent), it says "here is the desired state" (declarative, replayable, self-healing).
  Every robust distributed system of the last twenty years, from orchestrators to
  infrastructure-as-code tools, made that choice.
</Callout>

## Mechanism 3: the management marker

Last piece: the `managed_by` field. It indicates _who_ governs the entity: the control plane, an infrastructure-as-code tool, or nobody (manual creation).

Its role is first ergonomic: the service's interface can display "managed by the platform" and protect the entity against an accidental manual edit that would be overwritten at the next reconciliation. It is the answer to the classic problem of declarative systems: the local change silently reverted by the central manager, an endless source of confusion if nothing signals it.

## The federation contract

Let us recap what a service must expose to be "federable". It is remarkably little:

1. Local topological entities (`Project`, `Environment`...) carrying `external_ref` (unique, opaque) and `managed_by`.
2. An idempotent upsert by `external_ref` on those entities.
3. A published, versioned, documented API for those operations.

A service honoring this contract works alone, integrates with any control plane (the vendor's, an infrastructure-as-code tool, a homemade script), and the day a central referential is born, federation is _an API client to write_, not a migration.

Notice the symmetry with chapter 1: the federated model reuses the normalized-identifier idea from autonomous services (the `external_ref` plays the role of the global resource name), but adds what was missing: a **contractual local entity** the service can rely on, and a **designated owner** of the topology. And it reuses the monolith's consistency, but recomputed through convergence rather than guaranteed by a shared database.

## Check your understanding

<Quiz
  client:visible
  submitLabel="Submit"
  resetLabel="Reset"
  scoreLabel="Score"
  questions={[
    {
      q: "In the federated model, who is the source of truth for the 'project' concept?",
      options: [
        'Each service, independently',
        'The control plane for the topology; each service only keeps a local projection',
        'A database shared by all services',
        'The oldest service of the platform',
      ],
      answer: 1,
      explain:
        'The sharing of sovereignty is the heart of the model: the control plane owns the topology (orgs, projects, environments), each service owns its business resources and maintains a local projection of the topology that concerns it.',
    },
    {
      q: 'Why must external_ref stay opaque for the service storing it?',
      options: [
        'For security reasons: the URI contains secrets',
        'Because databases cannot index structured strings',
        'Because an interpreted identifier becomes a schema coupling between systems, impossible to evolve',
        'To save storage space',
      ],
      answer: 2,
      explain:
        "As soon as a service parses the reference's structure, its format becomes a contract shared by everyone: evolving it breaks everybody. Kept opaque, the reference is just a stable link, and a link never breaks.",
    },
    {
      q: 'What is the decisive contribution of idempotence in reconciliation?',
      options: [
        'It speeds up database writes',
        'It allows replaying the desired state without risk, turning synchronization into self-healing convergence',
        'It guarantees a message is never lost on the network',
        'It avoids having to version the API',
      ],
      answer: 1,
      explain:
        'Idempotence removes the need for exactly-once delivery: being able to replay is enough. The control plane re-declares the desired state whenever it wants, and the services converge, like a configuration management tool towards its manifest.',
    },
    {
      q: 'What is the managed_by field for?',
      options: [
        "Authenticating the control plane's calls",
        'Signaling who governs the entity and protecting against manual edits doomed to be overwritten',
        'Storing the modification history',
        'Encrypting the local projection',
      ],
      answer: 1,
      explain:
        'It answers the classic trap of declarative systems: the local change silently reverted at the next reconciliation. managed_by makes governance visible and lets the interface warn the user.',
    },
  ]}
/>

## Key takeaways

- The federated model shares sovereignty: **the control plane owns the topology, each service owns its resources** and maintains a local projection of the topology.
- An assumed projection is not duplication: it has a designated owner and an explicit convergence mechanism.
- Three mechanisms suffice: **opaque external reference** (the link without coupling), **idempotent upsert** (replayable convergence), **management marker** (visible governance).
- A service implementing this contract runs fully autonomously and federates without migration the day a control plane appears.
- One question remains: what do services actually share with each other, if neither code nor schema? The answer is conceptual, and Domain-Driven Design formulates it best. That is the subject of the last chapter.

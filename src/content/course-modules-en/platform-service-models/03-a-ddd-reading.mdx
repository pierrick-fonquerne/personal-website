---
course: platform-service-models
order: 3
title: 'A DDD reading: contexts, shared kernel and published language'
subtitle: "Why 'Project' does not mean the same thing in two services, why the shared types library is a trap, and what to standardize instead."
estimatedMinutes: 18
published: true
---

import Callout from '../../../components/learning/Callout.astro';
import Quiz from '../../../components/learning/Quiz.tsx';
import MermaidDiagram from '../../../components/learning/MermaidDiagram.tsx';

The first two chapters described _how_ to organize cross-cutting concepts. This last chapter explains _why_ the federated model is the right one, using the conceptual tools of Domain-Driven Design. Because DDD formalized, twenty years ago, exactly the problem we are handling: what happens when the same word designates different things depending on where you stand?

By the end of this chapter, you will be able to read a multi-service platform as a map of bounded contexts, recognize the shared kernel anti-pattern and its symptom (the distributed monolith), and formulate the three minimal conventions an ecosystem must publish to stay consistent without coupling itself.

## Same word, different meanings: the bounded context

Domain-Driven Design starts from a linguistic observation: in any organization, the meaning of a word depends on context. "Customer" does not designate the same thing for the sales department (a signed prospect), accounting (an account to invoice) and support (a contract holder). Forcing a single definition produces a monstrous model that satisfies nobody.

DDD names **bounded context** the boundary within which a model and its vocabulary are consistent and unambiguous. The central lesson: **do not unify the models, unify the map**. You accept several definitions of "customer", one per context, and you make the relations between contexts _explicit_.

Apply this grid to our platform:

- In the feature flag service, `Project` is _a namespace for flags_: it carries configuration environments and targeting segments.
- In the source code service, `Project` is _a grouping of repositories_: it carries read and write permissions on code.
- In the project management service, `Project` is _a backlog_: it carries tickets, milestones, priorities.

Same name, same intuition, **three definitions**. They share a link (it is "the same" project in the user's eyes), but neither their attributes, nor their lifecycle, nor their invariants coincide. Each service is a bounded context, and that is perfectly fine.

<MermaidDiagram
  client:visible
  definition={`flowchart LR
    subgraph BC1[Context: feature flags]
      P1["Project =<br/>flag namespace"]
    end
    subgraph BC2[Context: source code]
      P2["Project =<br/>repository group"]
    end
    subgraph BC3[Context: project management]
      P3["Project =<br/>backlog"]
    end
    P1 <-. "same link,<br/>not same definition" .-> P2
    P2 <-. " " .-> P3`}
  caption="Three bounded contexts: a shared name, three distinct models, connected by an identifier"
/>

## The anti-pattern: the involuntary shared kernel

Facing these three `Project`s, the temptation from chapter 1 comes back in a sneakier form: "since the services are separate, let us at least share _the types_". A common library, `platform-core`, defining the `Project` structure once and for all, imported by every service.

DDD calls this pattern the **shared kernel**: a model fragment jointly owned by several contexts. And it comes with a warning the industry learned the hard way: a shared kernel is only viable between very close teams, continuously coordinated, on a tiny scope. Everywhere else, it produces the worst of both worlds:

- Every evolution of the shared type requires re-versioning and re-deploying **all** the services importing it: you have recreated the monolith's evolution coupling.
- But without the monolith's benefits: no common transactions, no native integration, and the operational complexity of distribution on top.

This outcome has a name: the **distributed monolith**. Services technically separated, logically welded together. You pay the price of both architectures and collect the dividends of neither.

<Callout kind="warn" title="The shared kernel test">
  Before sharing a type between services, ask a single question: "if this type changes, am I ready
  to redeploy all the services the same day?" If the answer is no, this type must not be shared. The
  boundary of independent deployment is also the boundary of an independent model.
</Callout>

Note the nuance: publishing a types library _from one service, towards its clients_ (the flag service's contract, consumed by whoever wants to call it) is healthy: the dependency follows the direction of the call and the provider versions its contract. What is toxic is the type _owned by nobody and imported by everybody_.

## The healthy pattern: the published language

If you share neither a database (chapter 1) nor types (above), what is left? DDD's answer is called the **published language**: a published exchange language, stable, versioned, that each context translates to and from its internal model.

The canonical examples are the great interoperability standards: federated identity protocols, normalized event formats, open API specifications. None shares internal types; all publish a _boundary format_ that everyone implements at home.

For our federated platform, the published language takes a concrete and minimal form, in three conventions:

1. **A vocabulary and a minimal semantics.** What a "project" is for the ecosystem: a stable key, a display name, membership in an organization. What an "environment" is: a key and a criticality attribute. Nothing more: each context enriches _locally_.
2. **A normalized identifier scheme.** The format of external references (`platform:org/{org}/project/{key}`...): structured for the control plane that emits them, opaque for the services that store them. The equivalent of the cloud providers' global resource names.
3. **The federation contract.** Every building block exposes an idempotent upsert by external reference on its topological entities, documented in a versioned API specification.

One documentation page is enough to fix these three conventions. It is the most profitable investment of the whole architecture: it costs one decision, and it saves years of coupling.

<Callout kind="note" title="What circulates between contexts">
  Remember the hierarchy: between bounded contexts, you **never** circulate internal structures, you
  circulate **identifiers** (opaque, stable) and **messages** conforming to the published language
  (versioned, translated at the boundary). The identifier links, the message synchronizes, the model
  stays home.
</Callout>

## The complete map

You now hold the complete reading of this course:

| Question                         | Answer                                 | Conceptual tool            |
| -------------------------------- | -------------------------------------- | -------------------------- |
| Who owns the topology?           | The control plane                      | Federation (ch. 2)         |
| Who owns the business resources? | Each service                           | Bounded context            |
| What does "Project" mean?        | One definition per context             | Local ubiquitous language  |
| What is shared between services? | Identifiers + conventions, never types | Published language         |
| How do we converge?              | Idempotent reconciliation              | Declarative desired state  |
| What do we never do?             | The types crate imported by all        | Shared kernel anti-pattern |

This grid is not reserved for development platforms. It applies to any product family that must sell piece by piece _and_ integrate as a suite: productivity tools, industrial systems, internal microservice ecosystems of a large organization. Everywhere, the same tension between integration and autonomy, and everywhere the same way out: sovereign contexts, a published language, declarative convergence.

## Check your understanding

<Quiz
  client:visible
  submitLabel="Submit"
  resetLabel="Reset"
  scoreLabel="Score"
  questions={[
    {
      q: 'What does a bounded context designate in DDD?',
      options: [
        'A microservice deployed in a container',
        'The boundary within which a model and its vocabulary are consistent and unambiguous',
        "An application's security perimeter",
        'An isolated database',
      ],
      answer: 1,
      explain:
        'The bounded context is a boundary of meaning, not of infrastructure: inside it, every term has a single definition. A service is often its technical incarnation, but the concept is linguistic before being architectural.',
    },
    {
      q: 'Why is the types library shared between all services an anti-pattern?',
      options: [
        'Because it is hard to compile',
        "Because it recreates the monolith's evolution coupling without its benefits: the distributed monolith",
        'Because types cannot be serialized',
        'Because it prevents using several languages',
      ],
      answer: 1,
      explain:
        "Every type evolution forces the coordinated redeployment of all importers: the monolith's coupling, plus the operational complexity of distribution, minus the native integration. The worst of both worlds.",
    },
    {
      q: 'In the published language pattern, what circulates between contexts?',
      options: [
        "The models' internal structures, to avoid translations",
        'Opaque identifiers and versioned messages conforming to the published language',
        'Database schemas',
        "The services' source code",
      ],
      answer: 1,
      explain:
        'The internal model never leaves its context. At the boundary, each context translates to and from the published language; only identifiers (which link) and messages (which synchronize) circulate.',
    },
  ]}
/>

## Key takeaways

- Each service is a **bounded context**: "Project" has a legitimate local definition there. You do not unify the models, you unify the **map**.
- The involuntary **shared kernel** (the types library imported by all) produces the **distributed monolith**: the monolith's coupling, the distribution's complexity, the benefits of neither.
- The **published language** is the healthy pattern: minimal vocabulary, normalized identifier scheme, federation contract, all versioned and documented.
- Between contexts circulate **identifiers** and **messages**, never internal structures.
- The complete grid (federation + bounded contexts + published language + declarative reconciliation) applies to any ecosystem that wants to sell its building blocks piece by piece and integrate them as a suite.

---
course: platform-service-models
order: 2
title: Le modèle fédéré
subtitle: 'Un plan de contrôle qui possède la topologie, des services qui possèdent leurs ressources, et une réconciliation idempotente entre les deux.'
estimatedMinutes: 22
published: true
---

import Callout from '../../../components/learning/Callout.astro';
import Quiz from '../../../components/learning/Quiz.tsx';
import MermaidDiagram from '../../../components/learning/MermaidDiagram.tsx';

Le chapitre précédent s'est terminé sur un dilemme : le monolithe intégré donne la cohérence mais interdit l'autonomie des briques ; les services autonomes donnent l'autonomie mais abandonnent la cohérence. Ce chapitre présente la synthèse qui réconcilie les deux, à une condition non négociable : accepter que le même concept existe _plusieurs fois_, à des niveaux de responsabilité différents.

À la fin de ce chapitre, tu sauras décrire le modèle fédéré, expliquer le partage des responsabilités entre plan de contrôle et services, et reconnaître les trois mécanismes techniques qui le rendent possible : la référence externe opaque, l'upsert idempotent et le marquage de gestion.

## L'idée centrale : séparer topologie et ressources

Le modèle fédéré repose sur un partage de souveraineté précis :

- **Le plan de contrôle** (control plane) est la source de vérité de la **topologie organisationnelle** : les organisations, les projets, les environnements, qui appartient à quoi, qui a accès à quoi.
- **Chaque service** est la source de vérité de **ses ressources métier** : les flags pour le service de feature flags, les dépôts pour le service de code, les releases pour le service de déploiement.

Et le pont entre les deux : chaque service maintient une **projection locale** des entités topologiques qui le concernent. Le service de feature flags a _sa_ table `project`, minimale, qui reflète le référentiel central quand un plan de contrôle existe, ou qui se remplit à la main quand le service tourne seul.

<MermaidDiagram
  client:visible
  definition={`flowchart TD
    CP[Plan de contrôle<br/><i>source de vérité : Org, Project, Environment</i>]
    CP -- "provisionne / réconcilie<br/>(APIs idempotentes)" --> A
    CP -- " " --> B
    CP -- " " --> C
    subgraph A[Service feature flags]
      A1[Project*, Environment*]
      A2[Flags ✦]
    end
    subgraph B[Service code source]
      B1[Project*]
      B2[Dépôts ✦]
    end
    subgraph C[Service déploiement]
      C1[Project*, Environment*]
      C2[Releases ✦]
    end
    style CP fill:#7c5cff,color:#fff`}
  caption="* projection locale du référentiel — ✦ ressources dont le service est la source de vérité"
/>

Relis bien le schéma : c'est la propriété la plus importante du modèle. **Chaque service reste complet et autonome** : sa projection locale de `Project` lui suffit pour fonctionner sans plan de contrôle. Et **la plateforme reste cohérente** : quand le plan de contrôle existe, c'est lui qui crée et synchronise ces projections. Le même binaire sert les deux scénarios : produit autonome ou brique intégrée.

<Callout kind="warn" title="La condition d'acceptation">
  Le modèle fédéré exige de renoncer à un dogme : « une donnée ne doit exister qu'à un seul endroit
  ». Ici, le projet existe dans le plan de contrôle ET dans chaque service. Ce n'est pas de la
  duplication accidentelle, c'est une **projection assumée**, avec un propriétaire clair (le plan de
  contrôle) et un mécanisme de convergence explicite (la réconciliation). La duplication non
  gouvernée est une dette ; la projection gouvernée est une architecture.
</Callout>

## Mécanisme 1 : la référence externe opaque

Comment le plan de contrôle retrouve-t-il « son » projet dans chaque service ? Par un champ dédié sur les entités topologiques de chaque service :

```text
Project {
  key:          "mon-app"          // identifiant local du service
  name:         "Mon application"
  external_ref: "platform:org/acme/project/mon-app"   // optionnel
  managed_by:   "control-plane"                        // optionnel
}
```

`external_ref` est un identifiant **posé par le système externe et jamais interprété par le service**. Le service l'indexe (avec une contrainte d'unicité), le restitue, mais n'en lit jamais la structure. C'est le pattern des annotations dans les systèmes d'orchestration de conteneurs : une mémoire que le gestionnaire externe se laisse à lui-même.

Pourquoi opaque ? Parce que toute structure interprétée devient un couplage. Si le service de flags _parsait_ l'URI pour en extraire le nom de l'organisation, le format de l'URI deviendrait un contrat de schéma entre tous les systèmes, impossible à faire évoluer. Opaque, il reste un simple lien : et un lien, contrairement à un schéma, ne casse jamais.

C'est exactement la stratégie des protocoles d'identité fédérée : l'identifiant de sujet y est défini comme une chaîne opaque. Vingt-cinq ans d'interopérabilité reposent sur cette opacité.

## Mécanisme 2 : l'upsert idempotent

Le plan de contrôle ne « crée » pas les projections : il **réconcilie**. L'API de chaque service expose une opération dont le contrat est : « assure-toi qu'un projet avec cette `external_ref` existe et ressemble à ceci ».

- Si aucune entité ne porte cette référence : création.
- Si une entité la porte déjà : mise à jour des champs projetés.
- Dans tous les cas : **rejouer l'opération ne change rien**. C'est la définition de l'idempotence.

Cette propriété change la nature de la synchronisation. Plus besoin de garantir qu'un message de provisionnement arrive exactement une fois (garantie coûteuse, souvent illusoire en distribué) : il suffit de pouvoir le rejouer. Le plan de contrôle peut re-déclarer l'état désiré complet à chaque démarrage, après chaque incident, périodiquement par sécurité. L'état des services **converge** vers l'état déclaré, exactement comme un système de gestion de configuration converge vers son manifeste.

<Callout kind="tip" title="Déclaratif plutôt qu'impératif">
  Note le renversement : le plan de contrôle ne dit pas « fais ceci » (impératif, fragile, dépendant
  de l'ordre), il dit « voici l'état désiré » (déclaratif, rejouable, auto-réparant). Tous les
  systèmes distribués robustes des vingt dernières années, des orchestrateurs aux outils
  d'infrastructure as code, ont fait ce choix.
</Callout>

## Mécanisme 3 : le marquage de gestion

Dernière pièce : le champ `managed_by`. Il indique _qui_ gouverne l'entité : le plan de contrôle, un outil d'infrastructure as code, ou personne (création manuelle).

Son rôle est d'abord ergonomique : l'interface du service peut afficher « géré par la plateforme » et protéger l'entité contre une édition manuelle accidentelle qui serait écrasée à la prochaine réconciliation. C'est la réponse au problème classique des systèmes déclaratifs : la modification locale silencieusement annulée par le gestionnaire central, source de confusion sans fin si rien ne la signale.

## Le contrat de fédération

Récapitulons ce qu'un service doit exposer pour être « fédérable ». C'est remarquablement peu :

1. Des entités topologiques locales (`Project`, `Environment`...) portant `external_ref` (unique, opaque) et `managed_by`.
2. Un upsert idempotent par `external_ref` sur ces entités.
3. Une API publiée, versionnée et documentée pour ces opérations.

Un service qui respecte ce contrat fonctionne seul, s'intègre à n'importe quel plan de contrôle (celui de l'éditeur, un outil d'infrastructure as code, un script maison), et le jour où un référentiel central naît, la fédération est _un client d'API à écrire_, pas une migration.

Remarque la symétrie avec le chapitre 1 : le modèle fédéré reprend l'idée des identifiants normalisés des services autonomes (l'`external_ref` joue le rôle du nom de ressource global), mais il y ajoute ce qui manquait : une **entité locale contractuelle** sur laquelle le service peut s'appuyer, et un **propriétaire désigné** de la topologie. Et il reprend la cohérence du monolithe, mais recalculée par convergence plutôt que garantie par une base partagée.

## Vérifie ta compréhension

<Quiz
  client:visible
  questions={[
    {
      q: 'Dans le modèle fédéré, qui est la source de vérité du concept « projet » ?',
      options: [
        'Chaque service, indépendamment',
        "Le plan de contrôle pour la topologie ; chaque service n'en garde qu'une projection locale",
        'Une base de données partagée par tous les services',
        'Le service le plus ancien de la plateforme',
      ],
      answer: 1,
      explain:
        'Le partage de souveraineté est le cœur du modèle : le plan de contrôle possède la topologie (orgs, projets, environnements), chaque service possède ses ressources métier et maintient une projection locale de la topologie qui le concerne.',
    },
    {
      q: 'Pourquoi external_ref doit-il rester opaque pour le service qui le stocke ?',
      options: [
        "Pour des raisons de sécurité : l'URI contient des secrets",
        "Parce que les bases de données n'indexent pas les chaînes structurées",
        "Parce qu'un identifiant interprété devient un couplage de schéma entre systèmes, impossible à faire évoluer",
        "Pour économiser de l'espace de stockage",
      ],
      answer: 2,
      explain:
        "Dès qu'un service parse la structure de la référence, son format devient un contrat partagé par tous : le faire évoluer casse tout le monde. Opaque, la référence n'est qu'un lien stable, et un lien ne casse jamais.",
    },
    {
      q: "Quel est l'apport décisif de l'idempotence dans la réconciliation ?",
      options: [
        'Elle accélère les écritures en base de données',
        "Elle permet de rejouer l'état désiré sans risque, transformant la synchronisation en convergence auto-réparante",
        "Elle garantit qu'un message n'est jamais perdu sur le réseau",
        "Elle évite d'avoir à versionner l'API",
      ],
      answer: 1,
      explain:
        "L'idempotence dispense de la garantie de livraison exactement-une-fois : il suffit de pouvoir rejouer. Le plan de contrôle re-déclare l'état désiré quand il veut, et les services convergent, comme un outil de gestion de configuration vers son manifeste.",
    },
    {
      q: 'À quoi sert le champ managed_by ?',
      options: [
        'À authentifier les appels du plan de contrôle',
        "À signaler qui gouverne l'entité et protéger contre les éditions manuelles vouées à être écrasées",
        "À stocker l'historique des modifications",
        'À chiffrer la projection locale',
      ],
      answer: 1,
      explain:
        "C'est la réponse au piège classique des systèmes déclaratifs : la modification locale silencieusement annulée à la réconciliation suivante. managed_by rend la gouvernance visible et permet à l'interface de prévenir l'utilisateur.",
    },
  ]}
/>

## Ce qu'il faut retenir

- Le modèle fédéré partage la souveraineté : **le plan de contrôle possède la topologie, chaque service possède ses ressources** et maintient une projection locale de la topologie.
- La projection assumée n'est pas de la duplication : elle a un propriétaire désigné et un mécanisme de convergence explicite.
- Trois mécanismes suffisent : **référence externe opaque** (le lien sans couplage), **upsert idempotent** (la convergence rejouable), **marquage de gestion** (la gouvernance visible).
- Un service qui implémente ce contrat fonctionne en autonomie complète et se fédère sans migration le jour où un plan de contrôle apparaît.
- Reste une question : que partagent réellement les services entre eux, si ce n'est ni du code ni un schéma ? La réponse est conceptuelle, et c'est le Domain-Driven Design qui la formule le mieux. C'est l'objet du dernier chapitre.
